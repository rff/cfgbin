[credential]
        helper = cache
	username = rfirmino
[color]
        diff = auto
        status = auto
        branch = auto
        interactive = auto
        ui = true
        pager = true
[push]
        default = matching
[core]
	editor = vim
	hooksPath = ~/.config/git/hooks
[alias]
	puff = pull --ff-only
	ll = log --oneline --decorate --all --graph
	ls = log --oneline --decorate --all --graph --stat
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
	st = status --short --branch
	sa = status --short --branch --ignored
	g = grep --line-number --heading --break

#[branch]
#        autosetuprebase = always
#[merge]
#        tool = mymeld
#        conflictstyle = diff3
#[mergetool "mymeld"]
#        cmd = meld --diff $BASE $LOCAL --diff $BASE $REMOTE --diff $LOCAL $BASE $REMOTE $MERGED

[user]
	name = Raoni Fassina Firmino
#	email = raoni.firmino@padtec.com.br
	email = rfirmino@padtec.com.br
