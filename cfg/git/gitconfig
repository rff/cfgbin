[credential]
        helper = cache
[color]
        diff = auto
        status = auto
        branch = auto
        interactive = auto
        ui = true
        pager = true
[push]
        default = matching
[core]
	editor = vim
[alias]
	puff = pull --ff-only
	ll = log --oneline --decorate --all --graph
	ls = log --oneline --decorate --all --graph --stat
	st = status --short --branch
#[branch]
#        autosetuprebase = always
#[merge]
#        tool = mymeld
#        conflictstyle = diff3
#[mergetool "mymeld"]
#        cmd = meld --diff $BASE $LOCAL --diff $BASE $REMOTE --diff $LOCAL $BASE $REMOTE $MERGED

